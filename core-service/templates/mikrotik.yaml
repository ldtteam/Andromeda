apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mikrotik
  namespace: argocd
spec:
  project: default
  destination:
    server: "https://kubernetes.default.svc"
    namespace: mikrotik
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
  sources:
    - repoURL: {{ .Values.repository.url }}
      path: core-charts/mikrotik-extras
      targetRevision: HEAD