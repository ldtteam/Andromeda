apiVersion: v1
kind: Service
metadata:
  name: mikrotik-backplane
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: 443
      targetPort: 8443
      protocol: TCP
    - port: 80
      targetPort: 8080
      protocol: TCP