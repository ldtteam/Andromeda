apiVersion: longhorn.io/v1beta1
kind: RecurringJob
metadata:
  name: default-backup
  namespace: longhorn-system
spec:
  cron: "0 0 * * SUN"
  task: "backup"
  groups:
  - default
  retain: 4
  concurrency: 2
  labels:
    group: default
